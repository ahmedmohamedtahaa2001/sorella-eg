{%- liquid
  assign s = section.settings
  assign coll = s.collection
  assign count = s.product_count
-%}
<section class="sec"{% if s.anchor_id != blank %} id="{{ s.anchor_id }}"{% endif %}>
  <div class="wrap">
    <div class="sec__head">
      <div>
        {%- if s.eyebrow != blank -%}<span class="eyebrow">{{ s.eyebrow }}</span>{%- endif -%}
        {%- if s.heading != blank -%}<h2>{{ s.heading }}</h2>{%- endif -%}
      </div>
      {%- if s.link_label != blank -%}<a class="link-arrow" href="{{ s.link_url | default: coll.url | default: '#' }}">{{ s.link_label }}</a>{%- endif -%}
    </div>
    <div class="pgrid">
      {%- if coll != blank and coll.products.size > 0 -%}
        {%- for product in coll.products limit: count -%}
          {% render 'sorella-product-card', product: product, variant: s.card_variant %}
        {%- endfor -%}
      {%- else -%}
        {%- for i in (1..count) -%}
          {% render 'sorella-product-card', variant: s.card_variant %}
        {%- endfor -%}
      {%- endif -%}
    </div>
  </div>
</section>

{% schema %}
{
  "name": "Bestsellers grid",
  "tag": "section",
  "settings": [
    { "type": "text", "id": "eyebrow", "label": "Eyebrow", "default": "The most loved" },
    { "type": "text", "id": "heading", "label": "Heading", "default": "Bestsellers this month" },
    { "type": "collection", "id": "collection", "label": "Collection" },
    { "type": "range", "id": "product_count", "label": "Products shown", "min": 2, "max": 12, "step": 1, "default": 4 },
    {
      "type": "select",
      "id": "card_variant",
      "label": "Card style",
      "default": "plus",
      "options": [
        { "value": "add", "label": "Add to bag (always visible)" },
        { "value": "cta", "label": "Add + Buy now" },
        { "value": "plus", "label": "Quick + add" },
        { "value": "default", "label": "Reveal on hover" }
      ]
    },
    { "type": "text", "id": "link_label", "label": "Link label", "default": "Shop all 60+" },
    { "type": "url", "id": "link_url", "label": "Link URL" },
    { "type": "text", "id": "anchor_id", "label": "Anchor ID", "default": "shop", "info": "Used for in-page links (e.g. #shop)." }
  ],
  "presets": [{ "name": "Bestsellers grid" }]
}
{% endschema %}
