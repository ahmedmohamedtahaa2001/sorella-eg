{%- comment -%} Customer addresses — list + add/edit/delete {%- endcomment -%}
<section class="sorella-account surface-ink">
  <div class="container container--read">
    <div class="sorella-account__head">
      <span class="t-eyebrow text-gold">{{ 'customer.account.addresses' | t }}</span>
      <h1 class="t-display-md">{{ 'customer.account.addresses' | t }}</h1>
      <a class="t-caption" href="{{ routes.account_url }}">← {{ 'customer.account.title' | t }}</a>
    </div>

    {%- comment -%} New address {%- endcomment -%}
    <details class="glass-panel sorella-account__card" style="margin-block-end: var(--space-lg);">
      <summary class="t-body-strong">Add a new address</summary>
      {%- form 'customer_address', customer.new_address, class: 'sorella-account__form', style: 'margin-block-start: var(--space-md);' -%}
        <div class="sorella-account__row">
          <div class="sorella-field"><label class="t-caption-strong" for="new_first">First name</label><input type="text" id="new_first" name="address[first_name]" autocomplete="given-name"></div>
          <div class="sorella-field"><label class="t-caption-strong" for="new_last">Last name</label><input type="text" id="new_last" name="address[last_name]" autocomplete="family-name"></div>
        </div>
        <div class="sorella-field"><label class="t-caption-strong" for="new_addr1">Address</label><input type="text" id="new_addr1" name="address[address1]" autocomplete="address-line1"></div>
        <div class="sorella-account__row">
          <div class="sorella-field"><label class="t-caption-strong" for="new_city">City</label><input type="text" id="new_city" name="address[city]" autocomplete="address-level2"></div>
          <div class="sorella-field"><label class="t-caption-strong" for="new_country">Country</label><select id="new_country" name="address[country]">{{ all_country_option_tags }}</select></div>
        </div>
        <div class="sorella-field"><label class="t-caption-strong" for="new_phone">Phone</label><input type="tel" id="new_phone" name="address[phone]" autocomplete="tel"></div>
        <button type="submit" class="btn btn--primary">Save address</button>
      {%- endform -%}
    </details>

    {%- for address in customer.addresses -%}
      <div class="glass-panel sorella-account__card" style="margin-block-end: var(--space-md);">
        <p class="t-body-sm text-mist">{{ address | format_address }}</p>
        <details>
          <summary class="t-caption text-gold">Edit</summary>
          {%- form 'customer_address', address, class: 'sorella-account__form', style: 'margin-block-start: var(--space-md);' -%}
            <div class="sorella-account__row">
              <div class="sorella-field"><label class="t-caption-strong" for="first_{{ address.id }}">First name</label><input type="text" id="first_{{ address.id }}" name="address[first_name]" value="{{ address.first_name }}"></div>
              <div class="sorella-field"><label class="t-caption-strong" for="last_{{ address.id }}">Last name</label><input type="text" id="last_{{ address.id }}" name="address[last_name]" value="{{ address.last_name }}"></div>
            </div>
            <div class="sorella-field"><label class="t-caption-strong" for="addr1_{{ address.id }}">Address</label><input type="text" id="addr1_{{ address.id }}" name="address[address1]" value="{{ address.address1 }}"></div>
            <div class="sorella-account__row">
              <div class="sorella-field"><label class="t-caption-strong" for="city_{{ address.id }}">City</label><input type="text" id="city_{{ address.id }}" name="address[city]" value="{{ address.city }}"></div>
              <div class="sorella-field"><label class="t-caption-strong" for="country_{{ address.id }}">Country</label><select id="country_{{ address.id }}" name="address[country]">{{ all_country_option_tags }}</select></div>
            </div>
            <div class="sorella-field"><label class="t-caption-strong" for="phone_{{ address.id }}">Phone</label><input type="tel" id="phone_{{ address.id }}" name="address[phone]" value="{{ address.phone }}"></div>
            <div class="sorella-account__links">
              <button type="submit" class="btn btn--primary btn--sm">Update</button>
              <a class="btn btn--text" href="#" data-address-delete="{{ address.url_to_delete }}">Delete</a>
            </div>
          {%- endform -%}
        </details>
      </div>
    {%- endfor -%}
  </div>
</section>

<script>
  document.querySelectorAll('[data-address-delete]').forEach(function (link) {
    link.addEventListener('click', function (e) {
      e.preventDefault();
      if (!confirm('Delete this address?')) return;
      var f = document.createElement('form');
      f.method = 'post'; f.action = link.getAttribute('data-address-delete');
      f.innerHTML = '<input type="hidden" name="_method" value="delete">';
      document.body.appendChild(f); f.submit();
    });
  });
</script>
