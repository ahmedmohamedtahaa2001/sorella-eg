{%- comment -%}
  sorella-quantity-selector (§9).
  Params: id (string), name (string, default 'quantity'), value (number, default 1), min (number, default 1)
{%- endcomment -%}
<div class="sorella-qty" data-quantity>
  <button type="button" data-qty-step="-1" aria-label="{{ 'products.product.decrease_quantity' | t }}">
    {% render 'sorella-icon', name: 'minus', size: 16 %}
  </button>
  <input type="number" id="{{ id | default: 'quantity' }}" name="{{ name | default: 'quantity' }}"
    value="{{ value | default: 1 }}" min="{{ min | default: 1 }}" inputmode="numeric"
    aria-label="{{ 'products.product.quantity' | t }}">
  <button type="button" data-qty-step="1" aria-label="{{ 'products.product.increase_quantity' | t }}">
    {% render 'sorella-icon', name: 'plus', size: 16 %}
  </button>
</div>
