.DS_Store
Thumbs.db
*.log
node_modules/
.shopify/

# Secrets — never commit
.env
.env.*

# QA / tooling artifacts
.playwright-mcp/
__pycache__/
scrim-check.jpeg

# Separate app repo (has its own .git)
sorella-eg-integration/

# Project material lives on the project-materials branch, not on main
# (Shopify GitHub sync imports the whole branch and chokes on non-theme bulk)
current website/
Refrences/
Project Planning/
Skills/
docs/
